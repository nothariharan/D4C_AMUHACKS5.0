🎯 Core Differentiators
1. "Reality Check" System

After the AI creates the plan, introduce a 48-hour "reality testing" period where users must complete one randomly selected beginner task
If they struggle or skip it, the AI recalibrates the entire roadmap based on actual behavior vs stated knowledge
This creates honest self-assessment and prevents the "motivation porn" trap most goal apps fall into

2. "Accountability Twins" Matching

Instead of generic community features, pair users with ONE other person who has a similar goal and deadline
They see each other's progress (not details, just completion %), creating natural accountability
When one person completes a task, the other gets a gentle nudge (not annoying notifications)
Makes it feel less lonely without becoming a social network

3. "Energy-Based Scheduling"

Don't just ask about deadlines—ask about their energy patterns ("When do you focus best? Morning/Night/Weekends?")
AI schedules cognitively demanding tasks during their peak hours
Lighter tasks during low-energy times
This respects human reality instead of treating everyone like productivity robots

🎨 Interface Innovations
4. "Soundtrack Mode"

Each major milestone node on the metro map has an associated "vibe" or mood
Users can enable lo-fi/ambient music that changes as they progress through different phases
The brutalism design + intentional audio creates a meditative, focused environment
Makes the journey feel like an experience, not just a checklist

5. "Ghost Mode" for Roadmap

Show a translucent "ghost" roadmap of where they would be if they were ahead/behind schedule
Not to shame—but to visualize alternate timelines
Creates healthy tension without typical progress bar anxiety
Fits the brutalist aesthetic perfectly (literal vs shadow self)

6. "Collapse View"

A single-page, text-only version of their entire roadmap that loads instantly
No animations, no graphics—just pure information
For when they need to reference quickly or are on slow connections
Shows respect for user time and bandwidth

🧠 Smart Planning Features
7. "Dependency Chains" Visualization

Not just linear progression—show which tasks unlock others
Some tasks can be done in parallel; others must be sequential
Uses your metro map perfectly: show branching/merging lines
Helps users understand why the order matters, not just follow instructions

8. "Plateau Predictor"

AI identifies points where most people struggle/quit for this specific goal
Marks these on the roadmap with a special indicator
Before reaching these points, gives users pre-emptive strategies
Shows you understand the psychological journey, not just the technical one

9. "Flexible Replanning"

Life happens—instead of just adjusting deadlines, offer "plan surgery"
"You missed 2 weeks. Show me 3 different ways to adapt: (A) Extend deadline (B) Cut scope (C) Intensify remaining weeks"
Makes the plan feel like a living document, not a guilt trip

💡 Unique Mechanics
10. "Proof of Work" Submissions

For key milestones, require users to upload evidence (screenshot of project, code snippet, etc.)
Not AI-checked, but stored as personal documentation
Creates a portfolio while learning
Sidebar shows thumbnail history of their journey

11. "Question Reversal"

After each completed section, users must generate 3 questions they would ask about this topic
Tests understanding better than answering questions
These get stored and resurface later as spaced repetition

12. "Time Capsule Messages"

When setting up a goal, users write a message to their future self
It's revealed only when they complete the goal (or after the deadline passes)
Creates emotional bookends to the journey

🎮 Subtle Gamification (Not Tacky)
13. "Silent Streaks"

Track consistency, but don't show it prominently
Only reveal streak count when they're about to break it
"You've worked 12 days straight. Take tomorrow off guilt-free" or "Don't break your 12-day streak"
Respects effort without creating toxic pressure

14. "Reverse Progress"

Instead of showing % complete, show "You're X skills away from your goal"
Countdown feels more tangible than count-up
Psychological framing: emphasizes proximity to finish line

🔧 Technical Polish
15. "Offline-First Architecture"

Entire roadmap works offline after initial load
Syncs when connection returns
Shows you respect their context (commutes, travel, unstable wifi)

16. "Export to Reality"

One-click export to: Google Calendar, Notion, plain text, or PDF
Acknowledges people have existing systems
Reduces friction for users who want to use JustAsk as planning tool but execute elsewhere



1. The Sidebar: "The Cartridge System" (Visual & UX)
The Problem: Standard sidebars look like ChatGPT chat histories. They feel disposable. The Solution: Treat each Goal not as a "Chat," but as a "Game Cartridge" or "Project File."

Visuals: In your Neo-Brutalist sidebar, represent each goal as a thick, physical-looking "Disk" or "Card."

The Interaction: When a user clicks a different goal (e.g., switches from "React Dev" to "UI Designer"), don't just reload the page. Play a "Eject & Insert" animation. The old map slides out, the new map "clunks" in.

Status Indicators: On the sidebar "Card," show a mini-health bar.

Green: You are on track.

Red: You missed your deadline.

Cobwebs: You haven't opened this goal in 3 days.

Why this isn't a wrapper: It gives "weight" to the goals. It feels like an operating system for your career, not a text generator.

2. The Map: "Fog of War" & "Node Decay" (Gamification)
The Problem: Seeing a massive 6-month roadmap is overwhelming (Analysis Paralysis). Also, static maps are boring. The Solution: Make the map alive.

Fog of War: Only show the first 3 major "Stations" clearly. The future stations (months 4-6) are "Blurred" or "Locked" with a lock icon. You cannot see the details until you complete the prerequisites. This forces focus on the now.

Node Decay (The "Rust" Mechanic): If a user generates a plan but doesn't check off a task for 5 days, the nodes start to visually "crack" or get "rusty" borders.

The Fix: To repair the node, the user has to do a quick "Refresher Quiz" (generated by AI).

Why this isn't a wrapper: It adds consequence to inaction. The app reacts to the user's laziness.

3. The "Bounty Hunter" Verification (Proof of Work)
The Problem: In most apps, you just tick a box to say "I did it." That’s cheating. The Solution: Turn "Task Completion" into a "Bounty Submission."

The Feature: You cannot simply click "Done" on a Task Node. You must "Submit Evidence."

Code Task: The modal asks for a GitHub Link. (Your app creates a visual "Verified" stamp if the link is valid).

Design Task: Drag and drop a PNG.

Theory Task: The AI generates a specific, hard question: "Explain how Event Bubbling works in your own words." The node only unlocks if the answer is correct.

Why this isn't a wrapper: It builds a Portfolio. By the end of the roadmap, the user hasn't just clicked boxes; they have a repository of verified evidence.

4. Sidebar Synergy: "The Cross-Pollination Node"
The Problem: Users have multiple goals (e.g., "Full Stack Dev" and "UI Designer") but treat them as separate silos. The Solution: The app identifies overlaps.

The Feature: If the user has two goals in the sidebar that share a skill (e.g., "CSS" exists in both), create a Special Glowing "Link Node" in the sidebar or on the dashboard.

The Pitch: "Hey, if you learn 'CSS Grid' today, you advance BOTH your Full Stack and UI Design roadmaps simultaneously."

Why this isn't a wrapper: This requires logic that understands the relationship between separate AI conversations, something standard chat interfaces don't do.

5. The "Boss Battle" (Milestone Exams)
The Problem: Moving from one module to another feels anti-climactic. The Solution: End every major "Metro Line" (e.g., The JavaScript Line) with a Boss Node.

The Feature: The Boss Node is a Time-Limited Challenge.

The UI: The screen turns dark/red (Panic Mode).

The Task: "You have 15 minutes to build a To-Do list logic in this code editor." (Embedded Sandbox).

The Stakes: If they fail, the next metro line does not unlock. They have to wait 24 hours to retry (or pay a "penalty" of reviewing previous nodes).

Why this isn't a wrapper: It provides an actual barrier to entry for the next level, simulating the pressure of a real job interview.


Looking at your current features, here's what you **must** build next to make JustAsk a complete, retention-focused product (not just a cool demo):

---

# 🎯 **MUST-HAVE Features (Priority Order)**

## **TIER 1: Core Retention (Build These First)**

### **1. Daily Check-In System** ⭐⭐⭐
**Why it's critical**: Without this, users forget you exist after Day 2.

**What to build**:
```
Daily "Today's Quest" Generator
├─ Auto-generates 1-3 bite-sized tasks from roadmap
├─ Adapts to user's stated energy/time availability
├─ Marks completion with time investment tracking
└─ Feeds into streak system
```

**Implementation**:
```javascript
// Daily quest logic
const generateDailyQuest = (roadmap, userProfile) => {
  const uncompletedTasks = roadmap.tasks.filter(t => !t.completed);
  const userTimeToday = userProfile.availableMinutesToday || 30; // Default 30min
  
  // Smart selection based on:
  // 1. User's typical completion rate
  // 2. Task estimated time
  // 3. Prerequisites met
  // 4. Spaced repetition schedule
  
  return {
    tasks: selectOptimalTasks(uncompletedTasks, userTimeToday),
    estimatedTime: calculateTotalTime(),
    motivationBoost: generatePersonalizedEncouragement()
  };
};
```

**UI Location**: 
- Top banner on roadmap view: "TODAY'S QUEST" (collapsible)
- Separate `/today` route for focused daily view

---

### **2. Spaced Repetition Review System** ⭐⭐⭐
**Why it's critical**: Learning without retention = wasted time. This is what separates you from tutorial hell apps.

**What to build**:
```
Smart Review Scheduler
├─ Tracks when skills were "learned"
├─ Resurfaces review tasks at optimal intervals (1d, 3d, 7d, 14d, 30d)
├─ Mini-quizzes or "proof of retention" challenges
└─ Shows "rust risk" indicator on old completed nodes
```

**Visual Design**:
- Completed nodes show a small `↻` icon when due for review
- Add "REVIEW QUEUE" section in sidebar showing upcoming reviews
- Review nodes appear as dotted-outline versions of original nodes

**Implementation**:
```javascript
// Spaced repetition intervals (Anki-style)
const calculateNextReview = (taskId, attemptNumber) => {
  const intervals = [1, 3, 7, 14, 30, 60]; // days
  const now = new Date();
  const nextReview = new Date(now);
  nextReview.setDate(now.getDate() + intervals[attemptNumber]);
  
  return {
    taskId,
    nextReviewDate: nextReview,
    intervalDays: intervals[attemptNumber],
    status: 'scheduled'
  };
};

// Add to task completion
const completeTask = (taskId) => {
  // ... existing completion logic
  scheduleReview(taskId, 0); // Start with day 1 review
};
```

---

### **3. Evidence-Based Completion (Make it Real)** ⭐⭐⭐
**Why it's critical**: Checkbox completion = self-deception. Proof = actual learning.

**What to build**:
```
Proof of Work System
├─ Screenshot upload (for visual projects)
├─ Code snippet paste (for programming)
├─ Short reflection prompt ("What did you learn?")
├─ Optional: AI verification of code/content
└─ Creates portfolio artifact automatically
```

**UI Flow**:
```
User clicks "Mark Complete"
↓
Modal appears:
┌─────────────────────────────────────┐
│ PROVE IT                            │
│                                     │
│ Upload evidence (optional but rec): │
│ [📎 Upload Screenshot/File]         │
│                                     │
│ What did you learn? (required):     │
│ [Text area - min 50 chars]          │
│                                     │
│ [SUBMIT PROOF] [SKIP (Mark as WIP)] │
└─────────────────────────────────────┘
```

**Storage**:
- Store evidence in Firebase Storage
- Link evidence URLs to task completion records
- Show evidence in profile as "portfolio highlights"

---

### **4. Time Investment Tracking** ⭐⭐
**Why it's critical**: "You've invested 47 hours into this goal" is incredibly motivating.

**What to build**:
```
Session Timer System
├─ Auto-starts when user begins a task
├─ Pauses when idle (5min inactivity)
├─ Manual start/stop controls
├─ Daily/weekly/total aggregation
└─ Visualization in profile
```

**Visual Design**:
- Floating timer widget (bottom-right): `⏱️ 23:45`
- Profile shows: "127 hours invested in Full Stack Development"
- Weekly breakdown chart

**Implementation**:
```javascript
// Timer system
const useTaskTimer = () => {
  const [isActive, setIsActive] = useState(false);
  const [seconds, setSeconds] = useState(0);
  const [lastActiveTime, setLastActiveTime] = useState(Date.now());
  
  useEffect(() => {
    let interval;
    
    if (isActive) {
      interval = setInterval(() => {
        const now = Date.now();
        const idleTime = (now - lastActiveTime) / 1000;
        
        if (idleTime < 300) { // 5 min idle threshold
          setSeconds(s => s + 1);
        } else {
          setIsActive(false); // Auto-pause
        }
      }, 1000);
    }
    
    return () => clearInterval(interval);
  }, [isActive, lastActiveTime]);
  
  // Track mouse/keyboard activity
  useEffect(() => {
    const resetIdleTimer = () => setLastActiveTime(Date.now());
    window.addEventListener('mousemove', resetIdleTimer);
    window.addEventListener('keydown', resetIdleTimer);
    
    return () => {
      window.removeEventListener('mousemove', resetIdleTimer);
      window.removeEventListener('keydown', resetIdleTimer);
    };
  }, []);
  
  return { seconds, isActive, setIsActive };
};
```

---

## **TIER 2: Differentiation Features**

### **5. "Reality Check" Calibration** ⭐⭐
**What to build**:
```
48-Hour Trial Task System
├─ After roadmap generation, assign ONE random beginner task
├─ User must complete within 48h
├─ If they struggle/skip → AI recalibrates entire roadmap
├─ If they ace it → Roadmap stays as-is
└─ Prevents "motivation porn" syndrome
```

**Flow**:
```
Roadmap generated
↓
"REALITY CHECK" modal:
┌────────────────────────────────────────┐
│ Before we continue...                  │
│                                        │
│ Complete this task in the next 48h:   │
│ "Build a basic HTML page with a form" │
│                                        │
│ This helps us calibrate your roadmap. │
│                                        │
│ [ACCEPT CHALLENGE] [SKIP (Not Rec)]   │
└────────────────────────────────────────┘
```

---

### **6. Weekly Digest Email** ⭐⭐
**What to build**:
```
Automated Progress Report
├─ Tasks completed this week
├─ Time invested
├─ Streak status
├─ Next week's focus area
├─ One motivational insight
└─ One-click "Mark Week Complete" button
```

**Example Email** (plain text, brutalist style):
```
JUST ASK - WEEKLY DIGEST
━━━━━━━━━━━━━━━━━━━━━━━━━

Week of Feb 3-9, 2026

YOUR NUMBERS:
✓ 12 tasks completed
⏱️ 8h 34m invested
🔥 7-day streak maintained

MOMENTUM:
You're 18% through "Become a Full Stack Developer"
On track to finish by May 15, 2026

NEXT WEEK:
Focus: JavaScript Fundamentals
Est. time: 6-8 hours

[OPEN MY ROADMAP]

P.S. You completed 3 more tasks than last week. Keep it up.

━━━━━━━━━━━━━━━━━━━━━━━━━
Unsubscribe anytime
```

---

### **7. "Accountability Twin" Matching** ⭐⭐
**What to build** (Phase 1 - Simple):
```
Anonymous Partner System
├─ Match users with similar goals + deadlines
├─ Show ONLY: Partner's % completion, streak, last active
├─ No chat, no profile sharing (pure accountability)
├─ Weekly comparison summary
└─ Opt-in only
```

**UI**:
```
Sidebar widget:
┌─────────────────────────┐
│ YOUR ACCOUNTABILITY     │
│ TWIN                    │
│                         │
│ Goal: Full Stack Dev    │
│ Progress: 24%           │
│ Streak: 12 days         │
│ Last active: 2h ago     │
│                         │
│ You: 18% ↑Keep up!      │
└─────────────────────────┘
```

---

## **TIER 3: Polish & Scale**

### **8. Roadmap Export/Share** ⭐
- Export as PDF (clean, printable)
- Share link (public, read-only roadmap view)
- "Clone this roadmap" feature

### **9. Mobile Optimization** ⭐
- Responsive metro map (touch-friendly)
- Simplified daily view for mobile
- Push notifications (opt-in)

### **10. AI Coach Chat** ⭐
- Contextual Q&A about current task
- "I'm stuck, help me understand X"
- Uses Gemini API with roadmap context

---

# 🔥 **Post-Firebase Integration Priorities**

## **Week 1-2: Foundation**
1. ✅ Firebase Auth (email/password)
2. ✅ Firestore data structure setup
3. ✅ Migrate localStorage → Firestore
4. ✅ Real-time sync across devices

## **Week 3-4: Retention Core**
1. Daily Quest Generator
2. Time Investment Tracking
3. Evidence-Based Completion

## **Week 5-6: Learning System**
1. Spaced Repetition Reviews
2. Reality Check Calibration
3. Weekly Email Digests

## **Week 7-8: Growth Features**
1. Roadmap Sharing
2. Accountability Twin (Beta)
3. Mobile Polish

---

# 📊 **Firebase Schema Design**

```javascript
// Firestore structure
users/{userId}
  ├─ profile
  │   ├─ displayName
  │   ├─ email
  │   ├─ createdAt
  │   ├─ streak
  │   ├─ totalTimeInvested
  │   └─ preferences
  │
  ├─ goals/{goalId}
  │   ├─ title
  │   ├─ deadline
  │   ├─ createdAt
  │   ├─ roadmap (denormalized for speed)
  │   └─ status
  │
  ├─ tasks/{taskId}
  │   ├─ goalId
  │   ├─ title
  │   ├─ completedAt
  │   ├─ timeInvested
  │   ├─ evidenceUrl (Firebase Storage)
  │   ├─ reflection
  │   └─ nextReviewDate
  │
  ├─ dailyQuests/{date}
  │   ├─ tasks[]
  │   ├─ completed
  │   └─ totalTime
  │
  └─ activityLog/{timestamp}
      ├─ action (task_completed, login, etc.)
      └─ metadata

// Firebase Storage
users/{userId}/evidence/{taskId}/{filename}
```

---

# 🎯 **What NOT to Build Yet**

- ❌ Video content library (too much upfront work)
- ❌ Live mentorship (scaling nightmare)
- ❌ Full social network (scope creep)
- ❌ Premium tiers (validate retention first)
- ❌ Mobile app (PWA is enough initially)

---

# ✅ **Success Metrics to Track**

```javascript
// Critical retention metrics
const keyMetrics = {
  D1_retention: 'Users who return day 2',
  D7_retention: 'Users who return week 2',
  D30_retention: 'Users who return month 2',
  
  avg_session_duration: 'How long they stay',
  tasks_per_session: 'Depth of engagement',
  weekly_active_users: 'Sustained usage',
  
  completion_rate: 'Tasks with evidence / total tasks',
  roadmap_completion: 'Users who finish their goal',
  time_to_first_task: 'Activation metric'
};
```

---

**The bottom line**: Focus on **retention over features**. Daily Quests + Spaced Repetition + Evidence-Based Completion are the trio that will make users actually *learn* and keep coming back. Everything else is secondary.

Which of these would you like detailed implementation plans for?



. The "Evidence Locker" (Real Proof of Work)
Current State: You have a "Mock" completion flow. The Upgrade: Users must upload actual files to mark a task as done.

Why it's a Must-Have: It stops users from cheating themselves (the "Tutorial Hell" problem). It builds a real portfolio they can show employers.

Firebase Integration:

Use Firebase Storage to store screenshots/code snippets.

Save the downloadURL in the Firestore Task document.

The "Neo-Brutalist" Twist:

Don't use a standard upload bar. Make it a "Drop Zone" with a heavy dashed black border.

When a file uploads, stamp it with a massive "VERIFIED" watermark animation.

2. The "Cartridge" System (Multi-Goal Management)
Current State: Likely supports one active goal or messy local state. The Upgrade: A robust Sidebar that manages multiple active "Career Paths" simultaneously.

Why it's a Must-Have: Users are complex. A "Full Stack Dev" might also want to learn "UI Design." You need to support parallel progression.

Firebase Integration:

Structure: users/{uid}/goals/{goal_id} (Sub-collection pattern).

This allows you to load only the active map while keeping others in the DB (saving reads).

The "Neo-Brutalist" Twist:

Style the sidebar items like Game Cartridges or Floppy Disks.

Clicking one triggers a "Disk Insert" sound effect and a screen shake transition, as if loading a new program.

3. The "Blueprint" Exchange (Social Forking)
Current State: Roadmaps are isolated to the user. The Upgrade: Allow users to "Publish" their customized roadmaps and "Fork" others.

Why it's a Must-Have: It solves the "Cold Start" problem. New users shouldn't always have to generate from scratch; they should be able to grab the "Top Rated React Roadmap" created by a senior dev.

Firebase Integration:

Create a public_blueprints collection.

The "Fork" button runs a Transaction that copies the blueprint data into the user's goals collection.

The "Neo-Brutalist" Twist:

Call it "Steal This Roadmap" instead of "Fork."

Show a "Download Counter" that looks like an old-school website hit counter (odometer style).

4. The "Rust" Mechanic (Node Decay)
Current State: Static completion (Once done, always done). The Upgrade: Skills should "decay" if not practiced, forcing users to return.

Why it's a Must-Have: It creates Retention. Users have to log in to "maintain" their skills, or they lose their streak/status.

Firebase Integration:

Store a last_practiced_at timestamp on every Main Node.

On load, check: if (today - last_practiced > 7 days) { setStatus('decayed') }.

The "Neo-Brutalist" Twist:

Don't just turn the node red. Apply a "Cracked Glass" or "Rust" texture overlay to the node.

To "Repair" it, the user must answer one rapid-fire question (Gemini generated).

Summary Roadmap
Day 1: Migrate Zustand persist logic to Firestore (Real Sync).

Day 2: Build the "Evidence Locker" (Firebase Storage).

Day 3: Build the "Cartridge Sidebar" (Multi-goal architecture).

This transforms "Just Ask" from a tool into a platform.



1. Real Goal Threads (Cloud-Native Core)

Why: This is the soul of your product. Without this, everything resets.

What to build

Each goal = a persistent thread

Structure:

users/{uid}/goals/{goalId}
  - title
  - createdAt
  - deadline
  - status (active / paused / completed)
  - roadmapSnapshot


Sidebar = Goal Timeline

Active goals at top

Completed goals collapsed

Visual indicator of “last touched”

UX win

Switching goals feels like switching mental contexts, not pages.

2. Auth Upgrade: Identity Without Friction

You already have a smart “Progress Trap”. Now make it pay off.

Firebase Auth

Google (1-click)

Email (magic link preferred)

Key rule

Signing in should feel like unlocking memory, not creating an account.

Post-auth merge

Guest progress auto-merges into account

Zero reset, zero friction

3. True Progress Model (Beyond Checkmarks)

Right now: Tasks are visual
Next: Tasks become events

Firestore events

task_started

task_completed

task_skipped

task_revisited

Why

Enables:

Velocity accuracy

Burnout detection (later)

Smart AI adjustments

This is foundational for everything intelligent later.

Phase 2: Intelligence Layer (This Is Where You Win)

This is where Just Ask becomes something people can’t replace with ChatGPT.

4. Adaptive Roadmap Mutations 🧠

What

Roadmap is not static

It mutates based on:

Task failure

Skips

Long inactivity

Fast completion

Examples

User skips “Closures” twice → inject:

“JS Mental Model: Scope”

User completes 5 tasks fast → compress roadmap

UI hint

Node briefly pulses yellow when AI modifies it

Tooltip: “Adjusted based on your progress”

This creates trust, not magic.

5. Weekly Reflection Loop (Retention Engine)

Once per week

Auto-generated reflection:

What moved

What stalled

What changed in the plan

UI

Not a modal

A new node in the Metro Map:

“Weekly Review – Feb Week 2”

This turns progress into a narrative, not numbers.

6. Evidence-Based Completion (Make It Real)

You already mocked this. Now ship a v1.

Completion options

Paste link (GitHub, Notion, Docs)

Short text proof (“Explain in your own words”)

Optional screenshot upload

Why

Forces learning articulation

Makes streaks meaningful

Trains better AI adjustments

Phase 3: Emotional Stickiness (Most Apps Miss This)
7. Momentum States (Invisible but Powerful)

Behind the scenes, compute:

🚀 Momentum

😵 Friction

😴 Dormant

UI usage

Change microcopy tone

Change CTA wording

Change AI prompt style

Example:

High momentum → “Push further?”

Low momentum → “Stabilize, not sprint.”

No dashboards. Just felt intelligence.

8. Goal Endings (Most Products Ignore This)

When a goal finishes:

Ritual

Summary screen:

Time taken vs planned

Skills gained

“What surprised you?”

Archive

Goal moves to Hall of Wins

Visible on profile card

Completion should feel earned, not anticlimactic.

Phase 4: Differentiators (After Hackathon MVP)
9. Goal Dependencies (Advanced but Killer)

Example:

“Learn React” depends on “JavaScript Basics”

UI

Thin dotted lines between metro maps

Locked nodes with reason text

This elevates you from planner → thinking system.

10. AI as a Coach, Not a Generator

Shift Gemini prompts from:

“Generate roadmap”

to:

“Given this user’s behavior history, what is the next best intervention?”

That’s a huge conceptual leap.

What NOT to Build Yet (Important)

🚫 Social feeds
🚫 Leaderboards
🚫 Too many settings
🚫 Team goals

You’re building focus, not noise.